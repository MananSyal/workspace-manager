<div style="
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
">

  <form action="/login" method="POST" style="
    background:#ffffff;
    padding:40px;
    width:100%;
    max-width:420px;
    border-radius:16px;
    box-shadow:0 20px 40px rgba(0,0,0,0.08);
    font-family:Inter, sans-serif;
  ">

    <!-- Icon -->
    <div style="text-align:center; margin-bottom:20px;">
      <div style="
        width:56px;
        height:56px;
        background:#4f46e5;
        border-radius:12px;
        display:flex;
        align-items:center;
        justify-content:center;
        margin:0 auto 12px;
      ">
        <i class="fa-solid fa-server" style="color:white; font-size:24px;"></i>
      </div>
      <h2 style="margin:0;">Project Management System</h2>
      <p style="color:#64748b; margin-top:6px;">
        Secure Login (JWT Authentication)
      </p>
    </div>

    <!-- Error -->
    <% if (error) { %>
      <p style="color:#dc2626; text-align:center;"><%= error %></p>
    <% } %>

    <!-- Email -->
    <label>Email Address</label>
    <input
      type="email"
      name="email"
      required
      placeholder="you@example.com"
      style="width:100%; padding:12px; margin:6px 0 14px; border-radius:10px; border:1px solid #e5e7eb;"
    />

    <!-- Password -->
    <label>Password</label>
    <input
      type="password"
      name="password"
      required
      placeholder="••••••••"
      style="width:100%; padding:12px; margin:6px 0 18px; border-radius:10px; border:1px solid #e5e7eb;"
    />

    <!-- Button -->
    <button
      type="submit"
      style="
        width:100%;
        background:#4f46e5;
        color:white;
        padding:12px;
        border:none;
        border-radius:999px;
        font-weight:600;
        cursor:pointer;
      "
    >
      Sign In
    </button>

    <!-- Footer -->
    <p style="text-align:center; margin-top:18px;">
      Don’t have an account?
      <a href="/register" style="color:#4f46e5; font-weight:500;">Sign up</a>
    </p>

    <hr style="margin:22px 0; border:none; border-top:1px solid #e5e7eb;">

    <p style="text-align:center; font-size:12px; color:#64748b;">
      🔐 JWT Authentication · bcrypt Password Hashing
    </p>

  </form>
</div>
